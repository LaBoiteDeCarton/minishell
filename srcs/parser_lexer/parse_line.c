#include "ast.h"
#include "lexer.h"
#include "libft.h"

// int		is_scop(t_list	*lst)
// {
// 	if (((t_lxr *)lst->content)->type != scope_open)
// 		return (0);
// 	while (lst->next)
// 		lst = lst->next;
// 	if (((t_lxr *)lst->content)->type != scope_close)
// 		return (0);
// 	return (1);
// }




// t_ast	*from_lexer_to_ast(t_list *lst)
// {
// 	t_list	*ptr;

// 	ptr = lst;
	
// }